<!DOCTYPE html/>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bootstrap 101 Template</title>

    <!-- Bootstrap -->
    <!-- <link href="main.css.scss" rel="stylesheet"> -->
    <!-- <link href="style.css" rel="stylesheet" type="text/css"> -->

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>

<body>

    <!-- test delete before final -->
<div class="container">
    <!-- Begin Navigation Bar HTML -->
    <nav class="navbar navbar-default navbar-fixed-top" role="navigation" id="top">
        <div class="container">
            <div class="navbar-header">

                <!-- This is the hamburger button that appaears on the right hand side when window gets smaller-->
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <!-- End hamburger button -->

                <a class="navbar-brand" href="#">
                    <p id="brand">Global Champion</p>
                </a>
            </div>

            <!-- These are the buttons 'sign up and log in' -->
            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <% if user_signed_in? %>
                        <li class="active"><button type="button" class="btn btn-default btn-sm">Signed In as <%= link_to current_user.email %></button>
                          <%= form_for :session, url: destroy_user_session_path, method: 'delete' do |f| %>
                                <%= current_user.email %>
                        <button type="button" class="btn btn-default btn-sm">
                            <%= f.submit 'Sign Out', :class => 'button', id: 'signout' %> </button> </li>
                         <% end %>
                    <% else %>
                        <div>
                         <li class="active">
                    <%= link_to('Sign Up', new_user_registration_path )%></li>
                            <p>or</p>
                        <li class="active">
                    <%= link_to('Sign In', user_session_path ) %></li>
                        </div>
                    <% end %>
                </ul>
            </div>
            <!-- end 'sign up and log in' button -->
        </div>
    </nav>
    <!-- End Navigation Bar HTML -->

<div class="login">
            <h1 id="title">Global Champion</h1>
            <form id="login">
                    <% if user_signed_in? %>
                    <h3>Signed In as <%= link_to current_user.email %></h3>
                <% else %>

                        <h3><%= link_to('Sign Up', new_user_registration_path )%><p>or</p><%= link_to('Sign In', user_session_path ) %></h3>

                <% end %>
               <!--  <%= link_to('Sign In', user_session_path ) %>
                <input type="text" name="Username" placeholder="Username">
                <br>
                <input type="text" name="Password" placeholder="Password">
                <%= link_to('Password', user_password_path ) %>
                <br> -->
                <!-- <input type="password" name="pwd"> -->
                <!-- <br>
                <button type="button" class="btn btn-primary btn-lg active">Submit</button> -->
                <!-- <input type="submit" name="submit"> -->
            </form>
</div>
<div class="weapons">
    <div class="starter-template">

        <h1 id="headline">Choose your weapon!</h1>
        <div class="row">

            <div id="choices">
                <button id='rock_button' onClick='choose("rock"); compare(user, computerChoice); window.location.reload(true);'class='btn'><img src="assets/RockImg.jpeg"></button>

                <button id='paper_button' onClick='choose("paper"); compare(user, computerChoice); window.location.reload(true);'class='btn'><img src="assets/PaperImg.jpeg"></button>

                <button id='scissors_button' onClick='choose("scissors"); compare(user, computerChoice); window.location.reload(true);'class='btn'><img src="assets/ScissorsImg.jpeg"></button>
            </div>

            <br>
            <br>
            <div class="row">

            </div>
        </div>
    </div>
</div>
<div class="playagain">
            <br>
            <!-- javascript in html tags need to be refactored(separation of concerns) also, logic in Play again bttn wipes data from the page and reloads the page. need to refactor. -->
            <!-- <button onClick='window.location.reload(true)' class="btn" name="button">Play Again</button> -->

            <p id="result"></p>
           <br>
            <!-- <button class="btn" name="button">Sign up</button> -->


            <br>
            <br>
        </div>
    </div>
</div>







<script src="http://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>

</body>
</html>
