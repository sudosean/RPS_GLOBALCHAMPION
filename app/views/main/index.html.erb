<!DOCTYPE html/>
<body>
<div class="container">
            <h1 id="title">Global Champion</h1>
            <form>
                Username:
                <input type="text" name="Username">
                <br>Password:
                <input type="password" name="pwd">
                <br>
                <input type="submit" name="submit">
            </form>
            <br>
            <button onClick='choose("rock"); compare(user, computerChoice); window.location.reload(true);'class='btn'>Rock</button>
            <button onClick='choose("paper"); compare(user, computerChoice); window.location.reload(true);'class='btn'>Paper</button>
            <button onClick='choose("scissors"); compare(user, computerChoice); window.location.reload(true);'class='btn'>Scissors</button>
           
            <!-- javascript in html tags need to be refactored(separation of concerns) also, logic in Play again bttn wipes data from the page and reloads the page. need to refactor. -->
         
            <p id="result"></p>
            <br>
            <br>
            <br>
            <button class="btn" name="button">Sign up</button>

</div>

<script src="http://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>

</body>
